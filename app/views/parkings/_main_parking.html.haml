- parking = @parking
.main-parking-outer
  %h2= parking.internal_name
  .main-parking
    .main-parking-availabilities
      %h4
        Occupation actuelle
      .availability-content
        #{parking.total} #{t('place').pluralize}, #{t('including')}
        %span.main-free
          = parking.available
        #{free_label}.
      - [30, 60, 120].each do |delay|
        = render partial: 'forecast', locals: {parking: parking, delay: delay}
      .forecast-disclosure
        Données calculées à partir des occupations précédente du parking.
    .main-parking-info
      %h4
        Informations générales
    .main-parking-map
      %h4
        Emplacement
