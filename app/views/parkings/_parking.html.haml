%span.parking{data: {index: parking_counter}, class: parking.is_closed? && :closed}
  %h3.parking-name{data: {clean: parking.internal_name}, class: parking.pr? && :pr}
    = parking.base_name
    - if parking.pr?
      %br
      P+R
  .parking-data{class: parking.is_closed? && :closed}
    - if parking.is_closed?
      %span.available= t('closed')
      %br
    - else
      %span.available= parking.available
      %span.free= t('free').pluralize(parking.available)
      %br
    %span.total
      = "#{parking.total}"
      = t('place').pluralize
  - unless parking.is_closed?
    .parking-trend
      = trend_node(parking)
